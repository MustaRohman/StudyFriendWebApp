import React, {PropTypes} from 'react';

export default class SubjectList extends React.Component {
  static propTypes = {
  }
  constructor(props) {
    super(props);
  }

  state: {
    subjects: []
  }
  render() {
    return (
      <div>

      </div>
    );
  }
}
